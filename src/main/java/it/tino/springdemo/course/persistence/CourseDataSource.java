package it.tino.springdemo.course.persistence;

import it.tino.springdemo.DataSource;
import it.tino.springdemo.course.Course;

public interface CourseDataSource extends DataSource<Course, Integer> {
}
