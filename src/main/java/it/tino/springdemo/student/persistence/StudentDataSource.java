package it.tino.springdemo.student.persistence;

import it.tino.springdemo.DataSource;
import it.tino.springdemo.student.Student;

public interface StudentDataSource extends DataSource<Student, Integer> {
}
